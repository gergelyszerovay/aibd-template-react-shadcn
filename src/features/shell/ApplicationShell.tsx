import { Alert, AlertDescription, AlertTitle } from '@shared/shadcn-ui/Alert';
import * as React from 'react';

// Minimal application shell with a simple shadcn alert message
export function ApplicationShell(): React.ReactElement {
  return (
    <p class="font-bold p-5">
      ApplicationShell
    </p>
  );
}
